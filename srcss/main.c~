#include "test.h"

int		main(int ac, char **av)
{
	t_var	*s;

	ft_extern();
	ft_extern();
	ft_extern();
	ft_extern();
	ft_extern();
	s = g_var;
	while (s != NULL)
	{
		printf("[%d]\n", s->i);
		s = s->next;
	}
	printf("end\n");
	return (0);
}
